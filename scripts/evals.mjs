#!/usr/bin/env node
// Adversarial guardrail eval harness — placeholder.
//
// Wired now so the pipeline exists from the first commit and every later prompt or
// guardrail change has to clear it (see BUILD-GUIDE, Session 1 / Session 7).
// The real suite runs the corpus in PROJECT-PLAN §4.5 against live prompts and costs
// real API spend; it is not part of `pnpm test`.

console.log("evals: no cases defined yet (scaffold placeholder)");
process.exit(0);
